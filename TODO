1) plumb metadata k/v pairs through cli
2) add verification to the s3 downloader (sha check)
3) add utility to generate s3 download command line
4) add import/export utilities for ledger migrations/backups